import SearchResults from "../search/SearchResults";

const AllLocations = () => {
  return (
    <div className="all-elements">
      <h2>All Locations!</h2>
      <SearchResults />
    </div>
  );
};

export default AllLocations;
