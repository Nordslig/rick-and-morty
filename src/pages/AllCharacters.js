import DisplayContent from "../components/DisplayContent";

const AllCharacters = () => {
  return (
    <div className="contContent">
      <DisplayContent page="character" />
    </div>
  );
};

export default AllCharacters;
